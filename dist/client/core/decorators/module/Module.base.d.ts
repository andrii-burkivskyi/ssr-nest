import 'reflect-metadata';
import * as React from 'react';
import { AsyncLazyConstructor } from './module.extractor';
import { GuardBase } from '../guard/Guard.base';
import { ModulesListBase } from './ModulesList.base';
import { RequestsService } from '../../services/Requests.service';
declare class ModuleBase {
    static services: import("mobx").ObservableMap<any, any>;
    static list: (...modules: AsyncLazyConstructor<ModuleBase>[]) => ModulesListBase;
    constructor(parent?: ModuleBase);
    initStateEnd: () => void;
    waitForStateInit: Promise<unknown>;
    parent?: ModuleBase;
    requestService: RequestsService;
    View: Nullable<Constructable<React.Component<any>>>;
    model: Nullable<Object>;
    guard: GuardBase;
    children: Map<any, any>;
    services: Map<any, any>;
    get isActive(): boolean;
    get shouldDisplay(): boolean;
    private asyncInitModules;
    private asyncInitViewAndModel;
    private asyncInitServices;
    private init;
    private clear;
    private injectGuardDependencies;
    private injectModelDependencies;
    private injectDependencies;
}
export { ModuleBase };
