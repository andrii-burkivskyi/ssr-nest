import { ObservableString } from "../../utils/types";
import { CommandType } from "./translations.types";
export declare const commands: CommandType[];
export declare const t: (str?: ObservableString, values?: CommonMap) => string;
